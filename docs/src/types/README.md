# Custom Type Definitions

This directory is reserved for ambient declarations and shared TypeScript types that extend the game's runtime APIs. Add `.d.ts` files here as new systems transition to TypeScript.
