Original prompt: once complete start and finish the 5 most important issues

- 2026-03-29: Verified the sky-and-light pass with `npm.cmd run typecheck`, `npm.cmd run verify`, and a repo-local Playwright probe against preview. The live opening scene now reports a warm documentary sky setup (`zenith` `#5f93e0`, `horizon` `#ffd4a6`, fog near `950`, fog far `6750`, sun color `rgb(255,226,196)`, sun intensity `1.45`) while keeping the nearby `[E]` prompt and a clean browser console. Headless screenshot capture still timed out, so this pass was verified via live scene state instead of an image artifact.
- 2026-03-29: Started a sky-and-light atmosphere pass aimed at a brighter documentary Mediterranean look with subtle heroic warmth. Tuned the live daylight presets, dynamic sky palette, fallback sky defaults, and sun alignment so Athens opens with a cleaner blue zenith, warmer dusty horizon, softer cirrus, and a higher warm-white sun instead of the older low orange golden-hour look.
- 2026-03-29: Verified the terrain-density pass with `npm.cmd run typecheck`, `npm.cmd run verify`, and a repo-local Playwright probe against preview. The rebuilt terrain now reports about `294,912` triangles instead of the previous `524,288`, and the live stepped scene probe dropped total rendered triangles to roughly `361k` while keeping the nearby `[E]` prompt and a clean console.
- 2026-03-29: Started the next performance pass on terrain density. `src/world/terrain.js` now builds the main ground mesh at `384` segments instead of `512`, which should materially cut terrain render cost and the size of the merged environment collision geometry without changing the overall landform logic.
- 2026-03-29: Verified the city-detail pass with `npm.cmd run typecheck`, `npm.cmd run verify`, and a repo-local Playwright probe against preview. The live scene probe showed `CivicDistrict` dropping from roughly `4249` meshes to about `1345` while keeping the nearby `[E]` prompt intact and producing no browser console errors.
- 2026-03-29: Started the next scene-cost pass after profiling showed `CivicDistrict` was responsible for roughly `4249` meshes at startup. `src/world/cityPlan.js` now requests lower-detail building variants for the outer city fabric, and `src/world/buildingSpawner.js` trims expensive repeated house and warehouse detail meshes there while preserving the fuller Agora-core and sacred/civic presentation.
- 2026-03-29: Verified the interaction-hover pass with `npm.cmd run typecheck`, `npm.cmd run verify`, and a repo-local Playwright probe against preview. The browser probe kept the nearby `[E]` prompt visible before idle, after idle, and after `advanceTime(1000)`, with no console errors.
- 2026-03-29: Started the next always-on performance fix in `src/world/interactions.js`. The hover interactor now skips repeated center-screen raycasts when the camera pose and target list have not changed, while still forcing a short idle refresh so moving NPC prompts can recover without input.
- 2026-03-29: Verified the always-on runtime pass with `npm.cmd run typecheck`, a repo-local Playwright probe against a preview server, and `npm.cmd run verify`. The browser probe loaded the game and hooks without console errors and showed the opening snapshot around `651` draw calls dropping to about `599` after deterministic stepping, although Playwright's headless screenshot capture still times out in this environment.
- 2026-03-29: Started the always-on runtime audit fix pass after profiling showed the game was CPU-bound during movement. Cached the candidate lists for `src/utils/propCulling.js` and `src/utils/buildingCulling.js` so the recurring culling timers no longer rescan the whole scene graph every cycle, and tightened `src/audio/soundscape.js` so zone ambience only reconfigures when the active zone set actually changes.
- 2026-03-29: Added a live performance overlay to help diagnose the current stutter directly in-game. `src/ui/performanceHud.ts` now shows FPS, current/average/worst frame time, draw calls, triangles, textures, geometry count, and JS heap when the browser exposes it, mounted by default from `src/core/Application.ts` with `F10` as a quick hide/show toggle. The same snapshot is also exposed through `window.getPerformanceMetrics()` and included in `window.render_game_to_text()` for browser probes.
- 2026-03-29: Re-ran `npm.cmd run typecheck`, a direct Playwright preview probe, and `npm.cmd run verify` after the overlay pass. The first verify retry only failed because stale local preview servers were holding ports `4173`-`4176`; after clearing those listeners, full verification passed with no browser console or network errors. The direct browser probe confirmed the canvas renders, the panel stays visible at `1366x768`, and live stats are readable in both the HUD and `window.getPerformanceMetrics()`.

- 2026-03-28: Started a repo cleanup audit focused on overlapping world-building layers. Removed dead `src/world/ground.js`, `src/world/roads.js`, `src/world/plazas.js`, the unused `adjustShallowWater()` helper in `src/world/terrain.js`, the empty `HillCity` bootstrap stub, and the hidden duplicate harbor-city road mesh so the live runtime no longer carries those stale ground/road paths.
- 2026-03-29: Continued the city-stack cleanup by removing the dead `createHarborDecorations()` bootstrap path and its unused `harbor-decor` preset typing. The decoration layer was still wired to expect `harborCity` metadata that the live `createCity()` path no longer provides, so it was effectively inert.
- 2026-03-29: Continued the city-stack cleanup by making `createCity()` return `buildingPlacements` directly instead of hiding them inside `harborCity.userData`. `Application.ts` now passes that explicit metadata into terrain masking and ground-prop scatter, which narrows `city.js` to a clear runtime contract instead of a scene object with hidden side-channel data.
- 2026-03-29: Continued the city-stack cleanup by adding a `metadataOnly` path to `createCity()` and enabling it from `Application.ts`. The old organic harbor-city generator still computes `roadCurves` and `buildingPlacements`, but it no longer adds its own visible merged neighborhood geometry into the live scene, which removes another overlapping city-render layer while preserving the metadata other systems still consume.
- 2026-03-29: Continued the city-stack cleanup by extracting `createCityLayoutMetadata()` from `src/world/city.js` and switching `Application.ts` over to it directly. The live runtime no longer boots or threads a `harborCity` scene object at all; it now pulls only the `roadCurves` and `buildingPlacements` metadata it still needs, and `LightingSystem.js` no longer carries the dead `harborCity` update hook.
- 2026-03-29: Continued the overlap cleanup in `src/core/Application.ts` by making the procedural citizen crowd a fallback instead of a second always-on NPC layer. The runtime now prefers the GLB NPC walkers on the main road, and only spawns the simpler `spawnCitizenCrowd()` walkers if the GLB NPC path fails or returns no updaters.
- 2026-03-25: Repaired the repo verification workflow so it builds, launches a Vite preview server on a free localhost port, and checks the GitHub Pages base path in Playwright.
- 2026-03-25: Added `typecheck`, `preview`, `build:analyze`, and `verify` npm scripts; CI now uses `npm ci` and runs `typecheck` plus `verify` before deploy.
- 2026-03-25: Updated TypeScript config to allow the existing mixed TypeScript/JavaScript codebase to typecheck without placeholder module shims.
- 2026-03-25: Split release vs analyze build behavior so normal deploys use minification, treeshaking, and no sourcemaps, while analyze mode keeps the heavier debug settings.
- 2026-03-25: Synced the stale project docs with the current scripts and verification flow.
- 2026-03-25: Verified `npm run typecheck` and `npm run verify` both pass. Remaining notable risk: the production bundle is still over Vite's 500 kB warning threshold and could use chunking later.
- 2026-03-26: Updated the lockfile to pull in fixed versions of Vite, Rollup, minimatch, ajv, and lodash. `npm audit` now reports 0 vulnerabilities.
- 2026-03-26: Adjusted Vite manual chunking so Three.js and BVH code ship in dedicated vendor chunks. The build now emits a smaller app chunk and no longer produces chunk-size or circular-chunk warnings during `npm run verify`.
- 2026-03-26: Started the first demo content pass with a guided walking tour, district beacons for Agora/Harbor/Acropolis, and extra hero temple placements for the Agora and Acropolis.
- 2026-03-26: Verified the first demo pass with `npm run typecheck`, `npm run verify`, and browser inspection. Headless screenshots still mostly show HUD overlays, so future visual reviews may need headed capture or in-app debug camera framing.
- 2026-03-26: Started the second demo pass by strengthening each destination marker into a fuller arrival set piece with themed props and local light accents. Harbor is now the strongest active stop, while Agora and Acropolis read more distinctly on arrival.
- 2026-03-26: Verified the second pass with `npm run typecheck`, `npm run verify`, and browser inspection of quest state plus district marker composition counts.
- 2026-03-26: Started the third demo pass by switching debug overlays off at startup for a cleaner presenter-facing build, while keeping query flags available for manual debug sessions.
- 2026-03-26: Added a short intro card at scene open so first-time viewers immediately see the walking route and basic movement cue for the Athens demo.
- 2026-03-26: Verified the third pass with `npm run typecheck` and `npm run verify`. The production build regenerated `docs/` with the updated startup presentation.
- 2026-03-26: Started the demo choreography pass by moving the default spawn anchor to the west side of the Agora and setting the opening third-person camera to face east toward the harbor route.
- 2026-03-26: Verified the opening-spawn pass with `npm run typecheck`, `npm run verify`, and a scene-graph browser probe. The live player root now starts near `(-98, 3.9, 52)` with the camera placed just west of the player for a clearer route-facing opening.
- 2026-03-26: Started the Agora sightline pass by adding a dedicated route frame to the Agora marker and strengthening the harbor/acropolis marker label height plus focus-light placement for long-distance readability.
- 2026-03-26: Verified the sightline pass with `npm run typecheck`, `npm run verify`, and a browser scene probe. `AgoraSightlineFrame` is present in the live scene, and the harbor/acropolis marker labels now sit around `6.3` world units high for better readability from the Agora.
- 2026-03-26: Started the arrival-payoff pass by adding per-marker arrival halos and burst lights that pulse briefly when a demo stop is reached.
- 2026-03-26: Verified the arrival-payoff pass with `npm run typecheck`, `npm run verify`, and a browser scene probe. A live harbor arrival trigger now produces a visible halo pulse, scale expansion, and extra point-light intensity on the destination marker.
- 2026-03-26: Started the route-flow pass by adding stage-specific guide posts between Agora -> Harbor and Harbor -> Acropolis so the active demo leg reads more clearly in-world.
- 2026-03-26: Verified the route-flow pass with `npm run typecheck`, `npm run verify`, and a browser scene probe. The harbor leg now shows four active guide posts at startup while the four Acropolis guides remain hidden until the route advances.
- 2026-03-26: Restored the hero toward its original theme by clamping player-only reflection and gloss values after the GLB loads, instead of changing world lighting or other asset materials.
- 2026-03-26: Verified the hero material fix with `npm run typecheck`, `npm run verify`, and a browser material probe. Sampled player meshes now load with `envMapIntensity` `0.18`, `glossiness` `0.32`, and reduced specular color values rather than the previous highly reflective look.
- 2026-03-26: Switched GitHub Pages deployment from the branch-push `peaceiris/actions-gh-pages` flow to the official GitHub Actions Pages artifact flow (`configure-pages`, `upload-pages-artifact`, `deploy-pages`) to reduce extra Pages workflow noise and failure surface.
- 2026-03-26: Inspected the failed GitHub Actions build log and found the real breakage was missing Playwright browser binaries on the runner during `npm run verify`.
- 2026-03-26: Added an explicit `npx playwright install --with-deps chromium` step to the Pages workflow before typecheck/verify so the GitHub runner has the browser that `verification/verify.mjs` expects.
- 2026-03-26: Re-ran `npm.cmd run verify` locally after the CI change and it still passed, so the workflow fix did not break the current production verification path.
- 2026-03-26: Reviewed the leftover local changes and separated real source edits from generated noise. The useful keepers were a controls/HUD cleanup (`E` interact, `F` fly mode, arrow-key look help, fog hotkey text) plus a small manual-lighting responsiveness fix.
- 2026-03-26: Fixed the corrupted README text encoding while keeping the controls section aligned with the current playable input scheme.
- 2026-03-26: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` with the controls/HUD cleanup in place; both passed before discarding regenerated build artifacts.
- 2026-03-26: Started a ground texture cleanup pass after the terrain looked odd in demo play. Found that the terrain materials were still in a temporary stub state: all three terrain zones were rendering as the same unlit grass material.
- 2026-03-26: Restored proper terrain zoning so coast uses sand, city uses dirt, and inland uses grass, and switched those materials back to lit `MeshStandardMaterial` instances using the existing texture sets under `public/textures/`.
- 2026-03-26: Verified the ground-material pass with `npm.cmd run typecheck` and `npm.cmd run verify`. Headless canvas screenshots remain black in this environment, so visual confirmation is limited to code inspection plus successful browser load.
- 2026-03-26: Fixed a player-material regression where the hero looked metallic again after the ground-material pass. Root cause: global environment-intensity updates were overwriting the hero-specific reflection clamp after load.
- 2026-03-26: Tagged hero materials with an `envMapIntensityCap` during load and updated both lighting/environment intensity passes to respect that cap so preset changes no longer make the player shiny again.
- 2026-03-26: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the hero reflection fix; both passed.
- 2026-03-26: Started a harbor realism pass focused on demo impact. Added a proper quay edge with ladders and bollards, a larger hero ship with masts/sails/oars, and a more deliberate working waterfront zone with crane, market stalls, and cargo clustering.
- 2026-03-26: Kept the harbor pass inside `src/world/harbor.js` to avoid cross-system churn and preserve the existing tour marker/district logic.
- 2026-03-26: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the harbor pass; both passed.
- 2026-03-26: Started a compact-layout pass after the city still felt too large. Moved the harbor significantly closer to the civic core in the shared location constants, tightened the harbor water footprint, and nudged harbor ground height slightly upward so the harbor-city transition is less stretched.
- 2026-03-26: Kept the scale pass centered in `src/world/locations.js` so demo markers, roads, terrain carving, ocean bounds, and harbor placement all inherit the tighter layout automatically.
- 2026-03-26: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the compact-layout pass; both passed.
- 2026-03-26: Started a route-feel pass for the compact city layout by tightening the demo guide chains, nudging the harbor tour anchor, reshaping the main harbor-agora-acropolis boulevard, and refining the opening third-person camera framing so the first view reads more clearly into the harbor approach.
- 2026-03-26: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the route-feel pass; both passed.
- 2026-03-26: Started a harbor-atmosphere pass to make the port feel more like a working harbor. Added mooring ropes, fenders, rope coils, a gangplank for the hero ship, warehouse frontage, fishery clutter, and static dock-worker figures while keeping the work inside `src/world/harbor.js`.
- 2026-03-26: Updated shoreline dressing to use the current compact harbor water bounds instead of older hardcoded extents, so dockside props stay aligned with the smaller harbor footprint.
- 2026-03-26: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the harbor-atmosphere pass; both passed.
- 2026-03-26: Started the interaction-system cleanup by removing the duplicate `src/interactions/InteractionSystem.js` runtime path and routing the player's `E` input directly into the world interactor from `src/world/interactions.js`.
- 2026-03-26: Cleaned the remaining runtime wiring in `src/core/Application.ts` and `src/core/UIManager.js` so the world interactor is now the single interaction source of truth.
- 2026-03-26: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the interaction cleanup; both passed.
- 2026-03-26: Started the NPC-layer cleanup by choosing the path-based crowd system in `src/world/npcs.js` as the canonical runtime NPC path and removing the overlapping random-wander villager system from `src/world/traffic.js`.
- 2026-03-26: Cleaned `src/core/Application.ts` so it no longer creates or updates the duplicate `VillagerSystem`; the active NPC layer is now `spawnCitizenCrowd()` plus `spawnGLBNPCs()`.
- 2026-03-26: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the NPC cleanup; both passed.
- 2026-03-26: Started the environment/ocean boot cleanup by consolidating the duplicated ocean initialization in `src/core/Application.ts` down to a single canonical `createOcean()` path.
- 2026-03-26: Updated the surviving ocean initialization path to use the harbor water bounds and water-normal candidates directly, and removed the later dead fallback branch that would have called `createOcean()` with the wrong argument shape.
- 2026-03-26: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the ocean boot cleanup; both passed.
- 2026-03-26: Continued the environment cleanup by removing the unused `src/core/EnvironmentManager.js` layer. The active lighting bridge remains `src/core/EnvironmentManager.stubs.js`, which is the only environment-manager path referenced by `src/systems/LightingSystem.js`.
- 2026-03-26: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after removing the dead environment manager; both passed.
- 2026-03-26: Started a low-risk `src/core/Application.ts` cleanup pass by removing dead imports and old assembly leftovers left behind from earlier structural changes.
- 2026-03-26: Removed a duplicate `KeyE` interaction trigger from the window keydown handler so the world interactor now uses the player input queue as its single interaction path.
- 2026-03-26: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the `Application.ts` cleanup; both passed.
- 2026-03-26: Continued the application cleanup by extracting the manual runtime/debug controls into `src/core/ApplicationRuntimeControls.js`, so `src/core/Application.ts` now delegates the fog toggle, lighting cycle, hover-interact click, resize handling, and scene debug helpers instead of owning that block inline.
- 2026-03-26: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the runtime-control extraction; both passed.
- 2026-03-26: Started a tighter city-layout pass by pulling the shared harbor, agora, acropolis, and city-chunk anchors closer together in `src/world/locations.js` so the walk reads more like one organized city instead of several distant islands of content.
- 2026-03-26: Fixed a harbor-water terrain bug in `src/world/terrain.js` where north/south bound checks were reversed, which could stop the harbor basin from carving below sea level and make the water effectively disappear. The harbor basin now cuts deeper and the west-side shelf/slope use the corrected Z range.
- 2026-03-26: Started the next city-organization pass in `src/world/cityPlan.js` by tightening the civic block size, adding a clearer central Agora plaza, making the civic roads and footpaths render again, and fixing the harbor setback checks there so the street grid stops spilling into the wrong space.
- 2026-03-26: Nudged the main harbor-agora-acropolis road curve in `src/world/roads_hillcity.js` to suit the denser city footprint, with a shorter harbor approach and a straighter Agora-to-Acropolis climb.
- 2026-03-26: Started the next district-identity pass in `src/world/cityPlan.js` by adding a civic centerpiece to the Agora plaza, commercial awnings and banners, harbor-front cargo/canopy accents, and sacred column-and-altar accents so the main districts read differently at a glance.
- 2026-03-26: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the district-identity pass; both passed.
- 2026-03-26: Started a final demo-polish pass by tightening the opening spawn framing in `src/systems/PlayerSystem.js`, extending the intro card cue in `src/ui/demoIntro.js`, and adding stage-specific approach frames in `src/world/demoTour.js` so the harbor and Acropolis feel more intentionally staged on approach.
- 2026-03-26: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the demo-polish pass; both passed.
- 2026-03-26: Started an ocean-scale fix by separating the sheltered harbor basin from the seaward Aegean bounds in `src/world/locations.js`, then switching the runtime ocean mesh in `src/core/Application.ts` over to the larger open-sea bounds.
- 2026-03-26: Extended the terrain carve in `src/world/terrain.js` so the harbor still keeps its shallow basin while the connected seaward region drops deeper, making the harbor open into visible ocean instead of reading like an isolated water patch.
- 2026-03-27: Started a shoreline-polish pass by fixing `src/world/shoreTermination.js` to aim the coastal silhouette and water-haze arc at the current harbor position instead of the older layout, then strengthened the east-side horizon band in `src/world/horizon.js` so the open sea reads more clearly from inland.
- 2026-03-27: Expanded `src/world/backdrop/ShorelineDressing.js` to scatter more rocks and shoreline clutter around the harbor mouth and the first stretch of open sea, which should make the harbor-to-ocean transition feel less abrupt.
- 2026-03-27: Used the provided gameplay screenshot as a layout audit. It showed the Agora foreground was still overrun by random rocks and that sacred-scale massing was reading too close to the civic core.
- 2026-03-28: Started a screenshot-driven scene readability pass after the city still looked bland and plain. Softened the distant mountain backdrop in `src/world/backdrop/BackdropMountains.js` by switching from repeated sharp cone silhouettes to broader low-poly ridge forms with lit materials.
- 2026-03-28: Reworked `src/world/cityPlan.js` paved strips from thin boxes into flat planes so roads and footpaths stop drawing dark trench-like side faces across the city grid.
- 2026-03-28: Enriched `CityGroundMaterial` in `src/materials/groundMaterials.js` with gentler normal/AO detail and a warmer color balance so the city floor reads less like a single flat brown sheet.
- 2026-03-28: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the scene readability pass; both passed. A local Playwright capture succeeded but mostly caught the intro/loading card before the world view, so the final visual judgment still needs an in-browser hard refresh check from the user.
- 2026-03-28: Tightened third-person camera responsiveness after the camera still felt sluggish. Increased the runtime follow/rotation lerp in `src/systems/PlayerSystem.js` and removed extra keyboard-orbit easing in `src/controls/ThirdPersonCamera.js` so arrow-key camera turns respond more directly.
- 2026-03-28: Tightened the hero material override again after the player still looked metallic. In `src/systems/PlayerSystem.js`, reduced the hero env-map cap much further, pushed roughness higher, and zeroed out clearcoat/sheen-style reflection channels where present so the character keeps a softer cloth-and-leather look under the HDR environment.
- 2026-03-28: Found another water-vs-ground conflict in the render ordering. Updated `src/world/ocean.js` so the ocean surface renders after terrain and writes depth, and clarified the terrain ordering note in `src/world/terrain.js`, so submerged ground is covered by the water surface instead of drawing over it.
- 2026-03-28: Started an open-sea pass after the water still read like a lake. Extended `AEGEAN_OCEAN_BOUNDS` much farther east/north/south in `src/world/locations.js`, pushed the horizon/shore fade farther out in `src/core/Application.ts`, and adjusted `src/world/ocean.js` so the Aegean keeps more distant detail, stronger offshore color depth, and a livelier open-water surface.
- 2026-03-28: Continued the open-sea pass after the live view still felt boxed in. Reduced and pushed back the `BackdropMountains` coverage on the harbor side in `src/world/backdrop/BackdropMountains.js`, and expanded `AEGEAN_OCEAN_BOUNDS` again in `src/world/locations.js` so the visible sea sheet extends much farther beyond the harbor corridor.
- 2026-03-28: Fixed seabed material classification in `src/world/terrain.js` after underwater areas could still render with city dirt. Open-sea and underwater triangles now get routed to the coastal material instead of the city-ground material, which should stop brown dirt from showing through the water.
- 2026-03-28: Started an authored shoreline pass after the live edge still looked like dirt meeting water. Widened the harbor-front exclusion in `src/world/cityPlan.js` so the city grid steps farther back from the coast, and added a dedicated sand-and-rubble shoreline apron in `src/world/harbor.js` to turn the waterfront edge into a more deliberate beach/embankment transition.
- 2026-03-28: Started a compact-city pass after deciding the world still felt too large for an Ancient Greek city. Pulled the shared harbor/agora/acropolis anchors closer together in `src/world/locations.js`, reduced the compact harbor water footprint, tightened the civic block scale in `src/world/cityPlan.js`, and shortened the main harbor-agora-acropolis road staging in `src/world/roads_hillcity.js`.
- 2026-03-28: Stabilized `verification/verify.mjs` after the compact-city pass exposed a flaky preview readiness check. The verifier now allows a longer startup window, strips ANSI color codes from Vite output, and accepts the printed local preview URL as a ready signal before falling back to a direct HTTP probe.
- 2026-03-28: Started the shoreline sculpting pass by turning the harbor basin's west edge into a curved cove in `src/world/terrain.js` instead of a straight rectangular cut. The harbor shelf and shoreline-distance checks now follow that shaped coast, and `src/world/harbor.js` moves the sand apron and rubble dressing onto the same curved shoreline so the waterfront reads more naturally.
- 2026-03-28: Started the next harbor-identity pass in `src/world/harbor.js`. Added stone breakwater arms with beacon towers at the harbor mouth and a heavier quay forecourt with stepped stone terraces, cargo rows, and capstans so the waterfront reads more like an actual port instead of flat generic ground near the sea.
- 2026-03-28: Reduced rock clutter after the harbor and civic ground still felt too noisy. Lowered the default rock chance and total prop count in `src/world/groundProps.js`, cut shoreline rock count/scale in `src/world/backdrop/ShorelineDressing.js`, and trimmed the authored rubble clusters in `src/world/harbor.js` so the coast keeps texture without being covered in dark stones.
- 2026-03-28: Started a responsiveness/performance follow-up after movement became sluggish again. In `src/core/Application.ts`, the expensive prop/building culling passes now run on softer time intervals instead of every few frames, and in `src/world/harbor.js` the newer harbor detail groups were switched to receive-only shadows so they stop adding so much shadow-casting overhead.
- 2026-03-28: Removed rock clutter entirely from the visible world layers. `src/world/groundProps.js` no longer picks rock props, `src/world/backdrop/ShorelineDressing.js` no longer spawns shoreline rocks, and `src/world/harbor.js` no longer adds the small authored rubble clusters around the quay and shoreline apron.
- 2026-03-27: Updated `src/world/cityPlan.js` so district assignment is anchored to the real Agora, harbor, and Acropolis positions instead of a looser radial origin, and updated `src/world/groundProps.js` to keep random rocks/tufts/bushes out of the key district centers and reduce overall rock clutter.
- 2026-03-27: Started the clean civic composition pass by widening the Agora from a small central patch into a fuller plaza footprint in `src/world/cityPlan.js`, then adding perimeter civic accents so the square reads as bounded space while the middle stays open.
- 2026-03-27: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the Agora plaza pass; both passed.
- 2026-03-27: Continued the civic-composition work by fixing `src/world/cityPlan.js` so the Acropolis sacred district no longer spills so far into the Agora, adding a real `civic` ring around the plaza, and passing the matching district rule object into `spawnBuilding()` instead of the whole manifest.
- 2026-03-27: For the civic ring, filtered out temple spawns from the Agora-adjacent rule override so the plaza edge prefers stoas/monuments/plaza forms rather than large temple massing.
- 2026-03-27: Re-ran `npm.cmd run typecheck`, `npm.cmd run verify`, and a local Playwright client capture against `http://127.0.0.1:8000/athens-game-starter/` after the civic-ring pass; no browser error log was produced.
- 2026-03-27: Completed the next three demo-presentation passes with small focused edits: moved the opening spawn/camera farther west in `src/systems/PlayerSystem.js`, cleared a dedicated opening vista/foreground corridor in `src/world/groundProps.js`, and moved the startup harbor-stage frame in `src/world/demoTour.js` so the first view reads more like a composed civic scene.
- 2026-03-27: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the opening-view pass; both passed.
- 2026-03-27: Ran the local Playwright client against `http://127.0.0.1:8000/athens-game-starter/` after the opening-view changes. No browser error log was produced, but the headless screenshot remained a black canvas, which continues to look like an environment-specific capture limitation rather than a scene boot failure.
- 2026-03-27: Fixed a shoreline material clash by widening the coastal-material assignment in `src/world/terrain.js` so shallow-water and waterline triangles use the coastal sand material instead of letting city dirt reach all the way to the ocean edge.
- 2026-03-27: Softened `CoastalGroundMaterial` in `src/materials/groundMaterials.js` with a cooler sand tint, a slightly larger texture scale, and a gentler normal response so it sits more naturally beside the ocean surface.
- 2026-03-27: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the shoreline fix; both passed.
- 2026-03-27: Continued the shoreline pass in `src/world/backdrop/ShorelineDressing.js` by adding low beach patches and scrub clusters around the harbor mouth and first open-sea stretch so the coast reads as built-up shoreline instead of just a rock line.
- 2026-03-27: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the shoreline-dressing pass; both passed.
- 2026-03-27: Added a focused harbor-edge shoreline pass in `src/world/harbor.js` by building tapered north/south quay-end shoulders with sand aprons, embankment wedges, retaining stones, rubble, and mooring posts so the quay wall no longer stops as a hard square edge.
- 2026-03-27: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the harbor-edge transition pass; both passed.
- 2026-03-27: Used the latest gameplay screenshot as a material/readability audit. The Agora still had blown-out plaza stone and too many dark rock props scattered through the civic foreground.
- 2026-03-27: Tightened `src/world/groundProps.js` by reducing default scatter count, cutting rock odds, shrinking rock scale, and expanding the exclusion radius around the Agora/harbor so random clutter stays out of the main civic view.
- 2026-03-27: Softened the Agora stone treatment in `src/world/cityPlan.js` with warmer plaza tinting, lower normal intensity, lower environment reflection, and darker civic accent colors so the plaza reads as stone instead of flat white glare.
- 2026-03-27: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the Agora material/clutter pass; both passed. The local dev server at `http://127.0.0.1:8000/athens-game-starter/` also returned HTTP 200 during the check.
- 2026-03-27: Started the next city readability pass after the paths were still reading as black cuts through the scene. Softened `src/world/cityPlan.js` by flattening the paved-strip geometry, reducing its reflectivity, warming the road/footpath palette, and lowering the path offset above the ground so dark side faces are less visible.
- 2026-03-27: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the road/path material pass; both passed.
- 2026-03-27: Started the next Agora composition pass by tightening building massing near the civic center. `src/world/buildingSpawner.js` now respects `districtRules.heightRange` when spawning procedural buildings, and `src/world/cityPlan.js` now applies lower height ranges plus a small scale pass to civic/commercial buildings around the Agora.
- 2026-03-27: Kept the change narrow by only shrinking Agora-adjacent civic/commercial buildings instead of changing the whole city prefab set. Re-ran `npm.cmd run typecheck` and `npm.cmd run verify`; both passed.
- 2026-03-27: Started the next landmark-balance pass after the authored temple-scale landmarks were still competing with the Agora. Moved the Agora landmark farther west of the square and reduced its footprint, then nudged the Acropolis landmark farther into the summit zone with a slightly smaller footprint in `src/core/Application.ts`.
- 2026-03-27: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the landmark placement pass; both passed.
- 2026-03-27: Continued the Agora composition work by adding a dedicated north/south framing rule in `src/world/cityPlan.js`. The cells just outside the plaza now prefer low stoas/monuments, use tighter height ranges, scale down slightly more, and skip extra market clutter so the square feels framed without getting crowded.
- 2026-03-27: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the Agora framing pass; both passed.
- 2026-03-27: Continued the district-transition polish in `src/world/harbor.js` by dressing the city-harbor connector with small market stalls, amphorae, cargo, net bundles, and a worker at the city-side landing. The route from Agora to harbor should now pick up waterfront cues before the player reaches the quay.
- 2026-03-27: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the connector transition pass; both passed.
- 2026-03-27: Tightened the shoreline clip in `src/world/ocean.js` after water still appeared to fight with ground textures. The water shader now discards as soon as sampled terrain rises slightly above the waterline instead of tolerating up to 0.5m of overlap on land.
- 2026-03-27: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the ocean clip fix; both passed.
- 2026-03-27: Followed up on the remaining harbor water/ground clash in `src/world/harbor.js`. The harbor pad was still a centered 60x60 plane, so it could visually compete with the basin. Reshaped it into a narrower quay apron and offset it toward the built harbor edge instead of centering it across the water.
- 2026-03-27: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the harbor-pad adjustment; both passed.
- 2026-03-27: Started Phase 1 shoreline ownership cleanup by removing stale `HarborLowPolyWater` assumptions from `src/core/Application.ts` and `src/core/ApplicationRuntimeControls.js`, then updating `src/debug/groundAudit.js` so its notes match the current runtime model where `AegeanOcean` is the only live water surface.
- 2026-03-27: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify` after the Phase 1 cleanup. The first verify run hit a `page.goto(..., waitUntil: "networkidle")` timeout, but the immediate rerun passed without code changes, so it appears to have been a transient preview/load timing spike rather than a functional regression.
- 2026-03-27: Continued Phase 1 shoreline ownership cleanup by removing the broad `HarborPad` layer from `src/world/harbor.js`. The harbor basin edge now relies on `src/world/terrain.js` plus the existing quay, docks, and props instead of an overlapping opaque sand plane.
- 2026-03-27: Updated `src/debug/groundAudit.js` to document the new model more clearly: `AegeanOcean` remains the only live water surface, terrain owns shoreline ground near the basin, and harbor structures stay above that shoreline instead of replacing it.
- 2026-03-27: Continued Phase 1 in `src/world/terrain.js` by softening the harbor basin edge and tightening the harbor shelf corridor. The basin now slopes from shallow shoreline to deeper center, and the shelf behind the harbor fades by depth instead of reading as one broad rectangle.
- 2026-03-27: Continued Phase 1 shoreline material cleanup in `src/world/terrain.js` by adding a harbor-basin coastal band. Triangles near the harbor water bounds now classify as coastal ground when they are shallow or touching the waterline, instead of only relying on the main sea-edge distance logic.
- 2026-03-27: Started the visual follow-up pass by tightening `src/world/backdrop/ShorelineDressing.js` and `src/world/harbor.js`. The shoreline now uses fewer/smaller rock placements, fewer beach clusters on high ground, and a narrower harbor-edge prop scatter so the basin shape is easier to read.
- 2026-03-27: Used a fresh harbor screenshot to catch a coordinate bug in `src/world/cityPlan.js`. The civic district group was already positioned at the city center, but road/path/building tiles were still being placed using world-space cell coordinates, which let broad paved surfaces drift across the harbor; this pass converts those tile positions to local offsets before placement while keeping setback checks in world space.
- 2026-03-27: Followed up on a second harbor screenshot by expanding `HARBOR_SETBACKS` in `src/world/locations.js` to include the open Aegean bounds as well as the basin and walkway. This keeps the procedural city grid from rendering roads, paths, and buildings out over the visible sea sheet.
- 2026-03-27: Continued the harbor ownership cleanup in `src/world/cityPlan.js` by introducing an authored harbor-front exclusion zone. The generic civic grid now skips roads, paths, and building placement across the waterfront corridor that `src/world/harbor.js` already owns.
- 2026-03-27: Started structural cleanup Pass 1 in `src/core/Application.ts` by passing the live terrain mesh into `createHarbor(scene, { terrain })`. This enables the harbor's existing terrain-aware slot analysis without turning on the older sea-level resampling path that was still disabled in the boot flow.
- 2026-03-27: Continued structural cleanup in `src/world/cityPlan.js` by moving the harbor-front exclusion earlier into grid/path generation. Waterfront and sea-owned cells are now marked blocked before route planning, and the Harbor reachability target now points to the city-side harbor gate instead of the open waterfront.
- 2026-03-27: Finished the current cleanup pass by removing stale harbor config noise: dropped unused legacy harbor-size exports from `src/world/locations.js`, removed matching unused imports from `src/world/ocean.js`, and updated the outdated harbor-position comment in `src/world/harbor.js` so the docs point at the live `HARBOR_CENTER_3D` anchor instead of old coordinates.
- 2026-03-28: Started the next dedicated harbor visual pass. `src/world/cityPlan.js` now reserves a wider authored waterfront zone and replaces most surviving procedural harbor buildings with lower harbor-front accents, while `src/world/harbor.js` adds harbor-mouth markers and strengthens the hero ship silhouette farther toward the sea.
- 2026-03-28: Continued the harbor visual pass by teaching the older `src/world/city.js` HarborCity generator to skip the harbor basin, the open Aegean bounds, and a broader harbor-front corridor. This older layer still contributes buildings and placements used by harbor decoration, so excluding it stops small generic city geometry from being reintroduced underneath the authored waterfront.
- 2026-03-28: Reworked the older `src/world/city.js` building placement bands so they fit the compact map instead of using oversized legacy scatter rings. The HarborCity layer now keeps clear of the Agora and Acropolis cores, stays west of the harbor approach, and uses smaller inner/middle/outer neighborhood belts so buildings collect in more believable inland zones.
- 2026-03-28: Followed up on another harbor screenshot by fixing the terrain-to-water mask mapping in `src/world/ocean.js`. The shoreline clip was sampling the terrain height texture with world Z in the wrong orientation, so the ocean could cut against a mirrored heightfield and create hard-edged black/brown patches where water and ground appeared to fight.
- 2026-03-28: Started a runtime performance pass by reducing the default renderer/composer pixel ratio to 1.0, dropping the main sun shadow map from 2048 to 1024 in `src/world/lighting.js`, and shrinking the default Water reflection/refraction render target in `src/world/ocean.js` to a 256-based size. This should make the game noticeably less GPU-heavy on everyday machines without changing the scene layout.
- 2026-03-28: Followed up on movement/load feel in `src/systems/PlayerSystem.js` and `src/core/Application.ts`. The player now starts immediately with the fallback avatar while the hero GLB loads in the background, player meshes no longer receive shadows, the third-person camera follow/rotation lerp is more responsive, and the authored landmark placements now run in parallel instead of one after another during startup.
- 2026-03-28: Investigated the most obvious live issue from browser automation: Playwright captures were rendering Athens as an almost fully black frame even though the HUD and quest state were present.
- 2026-03-28: Updated `src/core/Scene.js` so WebDriver/browser-automation sessions preserve the drawing buffer and bypass the post-processing composer, while normal player sessions keep the existing bloom/color-grading pipeline.
- 2026-03-28: Updated the resize path in `src/core/Application.ts` to treat composer/bloom as optional when the automation fallback is active.
- 2026-03-28: Re-ran `npm.cmd run typecheck` and `npm.cmd run verify`; both passed. A fresh Playwright screenshot now captures the sky, mountains, labels, and city silhouettes instead of a featureless black screen, so browser-driven visual inspection is usable again.
- 2026-03-28: Started a walk-feel polish pass using the `develop-web-game` workflow. The shared skill Playwright client still cannot import `playwright` from its own skill directory in this environment, so I used the repo's local Playwright runtime for equivalent browser captures instead of blocking the work.
- 2026-03-28: Tightened the opening presentation in `src/ui/demoIntro.js` by reducing the intro card size and shortening its duration from 16s to 9s so it stops covering the scene for so long at the start of a walk.
- 2026-03-28: Refined opening third-person framing in `src/systems/PlayerSystem.js` by nudging the initial look target farther down-route, raising the target offset slightly, increasing the starting camera distance, and making follow/rotation settle a touch faster for a clearer, more readable first walk view.
- 2026-03-28: Re-ran `npm.cmd run typecheck`, `npm.cmd run build`, and `npm.cmd run verify`; all passed. Local Playwright movement/orbit captures still show no console errors, but this runtime still does not expose `window.render_game_to_text` or `window.advanceTime`, so browser automation coverage remains less rich than the web-game skill expects.
- 2026-03-28: Started the next web-game workflow fix by adding the missing browser automation hooks. `src/core/GameLoop.js` now supports deterministic manual time stepping through `advanceTime(ms)` without inventing a second update path.
- 2026-03-28: Updated `src/core/Application.ts` to expose `window.render_game_to_text()` and `window.advanceTime(ms)` using live player, camera, quest, time-of-day, prompt, and landmark target state from the actual runtime.
- 2026-03-28: Verified the new hooks with the repo's local Playwright runtime after waiting for the app to finish booting. The captured state now shows player coordinates, quest data, and time labels, and a stepped `KeyW` burst changes both time and player position in the text output with no browser console errors.
- 2026-03-28: Re-ran `npm.cmd run typecheck`, `npm.cmd run build`, and `npm.cmd run verify`; all passed. Remaining testing caveat: the shared skill client still cannot import `playwright` from its own directory here, so equivalent repo-local Playwright probes are still needed until that environment issue is resolved.
- 2026-03-28: Started the next opening-view readability pass using the `develop-web-game` workflow after a fresh baseline capture showed the giant cropped `Across The Agora` approach-frame label was dominating the top-right of the opening view.
- 2026-03-28: Updated `src/world/demoTour.js` so the harbor-stage approach frame sits farther down the route instead of almost on top of the spawn camera, and gave that specific frame a smaller label scale/height so it reads as a sightline cue rather than a full-screen overlay.
- 2026-03-28: Re-ran `npm.cmd run typecheck`, `npm.cmd run build`, and `npm.cmd run verify`; all passed. Local Playwright captures and stepped movement state showed no browser errors, the route frame still appears, and the opening view no longer has the giant cropped sign block in the top-right.
- 2026-03-28: Started the next opening readability pass after a fresh Playwright baseline still showed a very dark foreground wall across the lower half of the spawn view, plus a heavy left-side building silhouette near the Agora approach.
- 2026-03-28: Updated `src/world/cityPlan.js` to reserve a small paved arrival promenade on the north side of the west Agora approach, and nudged `src/world/lighting.js` daytime ambient values upward so the opening road and nearby stone read more clearly without changing the overall route layout.
- 2026-03-28: A follow-up visual check showed the opening camera was still reading too low into the foreground mass, so `src/systems/PlayerSystem.js` now starts the third-person camera a little higher, farther back, and pitched more steeply to reveal more of the road and route markers from the first frame.
- 2026-03-28: Another browser capture showed the spawn view was still collapsing into pure-black daylight shadows, so `src/world/lighting.js` now adds a modest hemisphere fill light that scales with time of day to restore readable ground bounce without flattening the whole scene.
- 2026-03-28: Widened the west-Agora arrival promenade reservation in `src/world/cityPlan.js` after the first corridor tweak still left a near-solid foreground wall in the opening view; the spawn-side forecourt now clears a broader patch north of the main avenue instead of just a few cells.
- 2026-03-28: Traced the strongest left-side opening silhouette back to the authored Agora temple placement in `src/core/Application.ts`; moved that landmark behind the spawn-side approach and reduced its footprint so it still exists as a local landmark without sitting directly in the first camera frame.
- 2026-03-28: Isolated the persistent mid-frame blocker to the harbor-side connector visible even with `WorldRoot` hidden. Shortened `createCityHarborConnector()` and shifted its threshold props east so the harbor ramp stays near the docks instead of stretching back across the opening Agora sightline.
- 2026-03-28: Continued the opening-view diagnosis with fresh Playwright scene-isolation captures. After hiding `WorldRoot` and then toggling direct scene children one by one, the remaining long black horizon band was traced to the top-level `AegeanOcean` mesh rather than another harbor prop group.
- 2026-03-28: Updated `src/world/ocean.js` so WebDriver/browser-automation sessions use a lightweight blue `MeshPhongMaterial` ocean fallback instead of the full `Water` shader. Normal player sessions still keep the existing animated water surface; the fallback only exists to stop automated opening captures from collapsing the sea into an opaque black strip.
- 2026-03-28: Re-checked the opening frame with the local Playwright workflow after the ocean fallback. The ocean now reads as water instead of a solid black band in automation, but the lower world geometry is still rendering much darker than expected in that browser-capture mode, so the next follow-up should inspect the automated material/lighting path for the remaining black foreground mass instead of moving more authored world geometry again.
- 2026-03-28: Followed up on the automation-only dark foreground by comparing WebDriver vs non-WebDriver captures, probing the center ray hit list, and temporarily swapping lit materials in-browser. That confirmed the remaining issue was the browser-capture material/lighting pipeline rather than another harbor blocker: the opening frame becomes readable as soon as standard/phong materials are replaced with simple preview shading.
- 2026-03-28: Updated `src/core/Scene.js` so WebDriver/browser-automation renders now apply a dedicated preview fallback before drawing the frame: disable dynamic shadows, add stronger ambient/hemisphere fill, and convert lit mesh materials to `MeshLambertMaterial` preview clones while leaving the normal player render path untouched.
- 2026-03-28: Re-ran the local Playwright opening capture after the `Scene.js` automation-preview fallback. The browser-driven screenshot now shows the Agora foreground, markers, player, and harbor approach as readable geometry instead of a near-solid black mass. Re-ran `npm.cmd run typecheck` and `npm.cmd run verify`; both passed.
- 2026-03-29: Continued the active city-stack cleanup by moving the last remaining legacy metadata generator out of `src/world/city.js` into the dedicated `src/world/legacyCityLayout.js` module. `src/core/Application.ts` now imports the explicit metadata-only layout helper, the old `src/world/city.js` file has been retired, and one stale `city.js` reference in `src/world/buildingSpawner.js` was updated to match the new ownership.
- 2026-03-29: Started shoreline ownership cleanup by removing the broad `HarborShorelineApron` layer from `src/world/harbor.js` and narrowing `src/world/backdrop/ShorelineDressing.js` so it decorates only the open-sea coast, not the harbor mouth. The harbor edge should now rely more directly on terrain plus ocean, with lighter shoreline decoration instead of competing sand/apron overlays.
- 2026-03-29: Continued shoreline cleanup in `src/world/shoreTermination.js` by removing the extra `ShoreTerminationBand` and `ShoreTerminationRocks` layers. That system now keeps only the distant water-horizon fade, so the visible coast is less split across multiple overlapping band/silhouette systems.
- 2026-03-29: Started the paved-surface cleanup in `src/world/cityPlan.js`. Roads, plazas, and footpaths still need to exist as authored paving, but they now sit much closer to the terrain and use a softer transparent material/pass so they read less like floating slabs cut over the ground.
- 2026-03-29: Started a compact-city pass in `src/world/cityPlan.js` using the latest screenshot as the guide. The grid now uses smaller blocks, a tighter Agora plaza, denser civic/market radii, and fewer generic road strips in residential/commercial districts so the layout should read more like continuous city fabric instead of isolated buildings around large paved gaps.
- 2026-03-29: Continued the city-composition pass in `src/world/cityPlan.js` by narrowing the central avenues, reducing generic road frequency again, shrinking the arrival promenade, and forcing more building cells around the Agora edge. The paving strips are also narrower now, so the square should feel framed by city blocks instead of dominated by broad flat slabs.
- 2026-03-29: Continued the Agora composition pass in `src/world/cityPlan.js` by widening the commercial ring and biasing the inner blocks toward stoas, shops, markets, and workshops instead of small houses. The framing buildings also stay slightly larger now, which should help the center read more like urban frontage and less like scattered tiny homes.
- 2026-03-29: Continued the city-composition pass in `src/world/cityPlan.js` by replacing the rigid repeating road cadence with staggered commercial and residential street patterns. The city grid should now form less of a checkerboard and read more like irregular joined neighborhoods.
- 2026-03-29: Started Phase 2 city-and-props work in `src/world/buildingSpawner.js` and `src/world/cityPlan.js`. Building forecourts now use warm stone paving instead of the old grassy placeholder color, shops/workshops/courtyards get more lived-in frontage details, and the inner residential ring can spawn benches, jars, and small olive-tree accents so the Agora edge reads more like inhabited city fabric.
